<div class="container">
  <div class="page-content">
    {% render 'breadcrumb' %}
    <h1 class="page-content__title">{{ page.title }}</h1>
    <div class="rte">{{ page.content }}</div>
  </div>
</div>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
